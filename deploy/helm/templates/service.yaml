apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-gateway
  labels:
    app.kubernetes.io/name: agentserve
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: agentserve
  ports:
    - name: http
      port: 80
      targetPort: {{ .Values.gateway.port }}
